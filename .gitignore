# Secrets (mots de passe Restic, credentials MinIO, .env)
secrets/
bookstack/.env
minio/.env

# Loop device image
*.img

# Fichiers générés par backup.sh (rapports JSON + métriques Prometheus)
reports/*
metrics/*
