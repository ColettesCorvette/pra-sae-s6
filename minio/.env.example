# Credentials de l'interface d'administration MinIO
# Copier ce fichier en .env et personnaliser les valeurs
# Ne jamais commiter le fichier .env (il est dans .gitignore)

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin_changeme_123
